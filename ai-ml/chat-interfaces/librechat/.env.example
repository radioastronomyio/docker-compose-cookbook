# LibreChat Configuration

# REQUIRED: Encryption keys for credential storage
# Generate with: openssl rand -hex 32
CREDS_KEY=
CREDS_IV=

# REQUIRED: JWT signing secret
# Generate with: openssl rand -hex 32
JWT_SECRET=

# Enabled endpoints (custom for local backends)
ENDPOINTS=custom
