# PrivateGPT Configuration

# Server port
PORT=8001

# Profile: ollama, local, or mock
PGPT_PROFILES=ollama

# Ollama connection (for ollama profile)
PGPT_OLLAMA_API_BASE=http://host.docker.internal:11434

# Embedding model for RAG
PGPT_EMBEDDING_MODEL_NAME=nomic-embed-text
