# Headscale Configuration
# Configuration is handled primarily via config.yaml in the mounted volume
# Download example: curl -o headscale_config/config.yaml https://raw.githubusercontent.com/juanfont/headscale/main/config-example.yaml
