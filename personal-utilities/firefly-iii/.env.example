# Firefly III Configuration
# APP_KEY must be exactly 32 characters
# Generate with: head /dev/urandom | tr -dc A-Za-z0-9 | head -c 32
APP_KEY=RandomStringOf32CharactersLength
DB_PASS=firefly_secure_pass
